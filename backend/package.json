{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../part2-notes-frontend/ && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui & git add . && git commit -m uibuild && git push origin Part3",
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js", 
    "test": "cross-env NODE_ENV=test nodemon --test",
    "start:test": "cross-env NODE_ENV=test nodemon index.js"
  },
  "author": "MarcusBruDev",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "latest-version": "^9.0.0",
    "mongoose": "^8.9.5"
  },
  "devDependencies": {
    "eslint": "^9.19.0",
    "nodemon": "^3.1.9",
    "supertest": "^7.0.0"
  }
}
